# Lab_22_K-Means-by-the-Algorithm
This lab practices K‑Means clustering—an unsupervised algorithm that partitions data into groups by iteratively assigning points to the nearest centroid and updating those centroids—using scikit‑learn sample data to understand the method and implement it in Python.
